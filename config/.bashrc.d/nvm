# technically, NVM_DIR should be set to "$HOME/.nvm" but nvm runs into
# all kinds of problems when NVM_DIR is a symlink
# see: https://github.com/creationix/nvm/issues/617

export NVM_DIR="$HOME/.dotfiles/config/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" # This loads nvm
